{
  "name": "lua-swap",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ minatofund/lua-swap",
    "create-local": "graph create --node http://localhost:8020/ minatofund/lua-swap",
    "remove-local": "graph remove --node http://localhost:8020/ minatofund/lua-swap",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 minatofund/lua-swap"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "0.19.0"
  },
  "version": "0.0.0"
}
